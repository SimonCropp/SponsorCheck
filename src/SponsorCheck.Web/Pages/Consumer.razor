@page "/consumer"
@namespace SponsorCheck.Web.Pages
<PageTitle>SponsorCheck Setup Wizard — consumer</PageTitle>

<div class="wizard">
    <Stepper Labels="steps" Active="step" />

    <div class="step-body">
        @switch (step)
        {
            case 0:
                <h2>Your situation</h2>
                <div class="field">
                    <label for="scCode">Diagnostic code from the failing build <span class="optional-tag">optional</span></label>
                    <p class="hint">The SC code in the build output encodes the answers below — enter it and the questions pre-answer themselves (still editable).</p>
                    <input id="scCode" type="text" placeholder="SC001"
                           @bind="model.EnteredCode" @bind:event="oninput" @bind:after="CodeChanged" />
                    @if (classification is { } parsed)
                    {
                        @if (!parsed.Recognized)
                        {
                            <p class="validation-error">Not a recognized SponsorCheck diagnostic code.</p>
                        }
                        else if (parsed.AuthorSide)
                        {
                            <p class="callout">@parsed.Note Switch to the <a href="author">author flow</a>.</p>
                        }
                        else if (parsed.Note is { } note)
                        {
                            <p class="callout">@note</p>
                        }
                        else
                        {
                            <p class="hint">Recognized — the questions below have been pre-answered.</p>
                        }
                    }
                </div>

                <div class="field">
                    <label>Configuration style</label>
                    <div class="radio-row">
                        <input type="radio" id="style-package" name="configStyle"
                               checked="@(!model.OwnerMode)" @onchange="() => model.OwnerMode = false" />
                        <label for="style-package">
                            Per package
                            <p class="hint">The metadata lives on the package reference itself (codes SC001–SC016 and most others).</p>
                        </label>
                    </div>
                    <div class="radio-row">
                        <input type="radio" id="style-owner" name="configStyle"
                               checked="@model.OwnerMode" @onchange="() => model.OwnerMode = true" />
                        <label for="style-owner">
                            Owner mode
                            <p class="hint">The error names a global property like <code>&lt;acme_GitHubSponsorAccount&gt;</code> (codes SC021–SC028) — one declaration covers every package from that owner.</p>
                        </label>
                    </div>
                </div>

                @if (model.OwnerMode)
                {
                    <div class="field">
                        <label for="ownerId">Owner id</label>
                        <p class="hint">Copy it from the property name in the error message — the part before the underscore, e.g. <code>acme</code> for <code>&lt;acme_GitHubSponsorAccount&gt;</code>.</p>
                        <input id="ownerId" type="text" placeholder="acme"
                               @bind="model.OwnerId" @bind:event="oninput" />
                    </div>
                }
                else
                {
                    <div class="field">
                        <label>Central Package Management?</label>
                        <div class="radio-row">
                            <input type="radio" id="cpm-no" name="cpm"
                                   checked="@(!model.Cpm)" @onchange="() => model.Cpm = false" />
                            <label for="cpm-no">
                                No
                                <p class="hint">Versions sit on each <code>&lt;PackageReference&gt;</code> (odd-numbered codes: SC001, SC007, …).</p>
                            </label>
                        </div>
                        <div class="radio-row">
                            <input type="radio" id="cpm-yes" name="cpm"
                                   checked="@model.Cpm" @onchange="() => model.Cpm = true" />
                            <label for="cpm-yes">
                                Yes
                                <p class="hint">Versions live in <code>Directory.Packages.props</code> (even-numbered codes: SC002, SC008, …).</p>
                            </label>
                        </div>
                    </div>
                }
                break;

            case 1:
                <h2>The package</h2>
                <div class="field">
                    <label for="packageId">
                        Package id
                        @if (model.OwnerMode)
                        {
                            <span class="optional-tag">optional</span>
                        }
                    </label>
                    <p class="hint">
                        @(model.OwnerMode
                            ? "In owner mode the properties are package-independent — the id is only used in the explanatory text."
                            : "The package named in the diagnostic — the snippet attaches to its reference.")
                    </p>
                    <input id="packageId" type="text" placeholder="ThePackage"
                           @bind="model.PackageId" @bind:event="oninput" />
                </div>
                <div class="field">
                    <label for="packageVersion">Version <span class="optional-tag">optional</span></label>
                    <p class="hint">Rendered on the snippet when present; leave blank to keep the existing Version attribute untouched.</p>
                    <input id="packageVersion" type="text" placeholder="1.0.0"
                           @bind="model.PackageVersion" @bind:event="oninput" />
                </div>
                break;

            case 2:
                <h2>License mode</h2>
                <p class="hint">The modes are mutually exclusive — pick exactly one.</p>
                <div class="mode-cards">
                    <button type="button" class="mode-card @CardClass(ConsumerLicenseMode.Sponsor)" @onclick="() => SetMode(ConsumerLicenseMode.Sponsor)">
                        <strong>Sponsor account</strong>
                        <span>Sponsoring the author (or about to)</span>
                    </button>
                    <button type="button" class="mode-card @CardClass(ConsumerLicenseMode.License)" @onclick="() => SetMode(ConsumerLicenseMode.License)">
                        <strong>Private license</strong>
                        <span>A licensing arrangement with the author</span>
                    </button>
                    <button type="button" class="mode-card @CardClass(ConsumerLicenseMode.Exemption)" @onclick="() => SetMode(ConsumerLicenseMode.Exemption)">
                        <strong>Exemption</strong>
                        <span>Claiming a carve-out the publisher defined</span>
                    </button>
                    <button type="button" class="mode-card @CardClass(ConsumerLicenseMode.Ignore)" @onclick="() => SetMode(ConsumerLicenseMode.Ignore)">
                        <strong>Ignore</strong>
                        <span>Proceed unlicensed, warning accepted</span>
                    </button>
                </div>

                @switch (model.Mode)
                {
                    case ConsumerLicenseMode.Sponsor:
                        <p class="hint">Enable each platform the sponsorship runs on and enter the sponsoring account. The verifier passes on any single match.</p>
                        @foreach (var platform in Platform.All)
                        {
                            var selection = model.Selection(platform.Kind);
                            <div class="platform-row">
                                <div class="check">
                                    <input type="checkbox" id="@($"sponsor-{platform.Kind}")" @bind="selection.Enabled" />
                                    <label for="@($"sponsor-{platform.Kind}")">@platform.DisplayName</label>
                                </div>
                                @if (selection.Enabled)
                                {
                                    <div class="account field">
                                        <label for="@($"sponsor-account-{platform.Kind}")">@platform.ConsumerAccountMetadata</label>
                                        <input id="@($"sponsor-account-{platform.Kind}")" type="text" placeholder="the sponsoring account"
                                               @bind="selection.Account" @bind:event="oninput" />
                                    </div>
                                }
                            </div>
                        }
                        <div class="check" style="margin-top:1rem">
                            <input type="checkbox" id="startedAfter" @bind="model.StartedAfterRelease" />
                            <label for="startedAfter">
                                The sponsorship started after this package version was released
                                <p class="hint">The bundled sponsor list is frozen at pack time, so a recent sponsorship can't be in it yet — an attested start date bridges the gap until the next upgrade.</p>
                            </label>
                        </div>
                        @if (model.StartedAfterRelease)
                        {
                            <div class="field">
                                <label for="sponsorshipStart">Sponsorship start date</label>
                                <input id="sponsorshipStart" type="text" placeholder="yyyy-MM-dd"
                                       @bind="model.SponsorshipStart" @bind:event="oninput" />
                                @if (model.SponsorshipStart.Trim().Length > 0 && !model.IsSponsorshipStartValid)
                                {
                                    <p class="validation-error">Use the format yyyy-MM-dd, e.g. 2026-04-30.</p>
                                }
                                else if (StartIsInFuture)
                                {
                                    <p class="callout">That date is in the future — the verifier rejects it (SC015 family). Use the real start date.</p>
                                }
                            </div>
                        }
                        break;

                    case ConsumerLicenseMode.License:
                        <div class="field">
                            <label for="licensedUntil">Licensed until (last covered month)</label>
                            <p class="hint">Format yyyy-MM; valid through the end of that month (UTC). Capped at one year out — a longer arrangement is renewed with a one-line edit.</p>
                            <input id="licensedUntil" type="text" placeholder="yyyy-MM"
                                   @bind="model.LicensedUntilMonth" @bind:event="oninput" />
                            @if (model.LicensedUntilMonth.Trim().Length > 0 && !model.IsLicensedUntilValid)
                            {
                                <p class="validation-error">Use the format yyyy-MM, e.g. 2027-06.</p>
                            }
                        </div>
                        break;

                    case ConsumerLicenseMode.Exemption:
                        <div class="field">
                            <label for="exemptionName">Exemption name</label>
                            <p class="hint">Exemptions are defined by the publisher at pack time — the failing build's error message lists the available names when an unknown one is claimed.</p>
                            <input id="exemptionName" type="text" placeholder="Consulting"
                                   @bind="model.ExemptionName" @bind:event="oninput" />
                        </div>
                        break;

                    case ConsumerLicenseMode.Ignore:
                        <p class="callout">
                            The build will pass but log a breach-of-license warning on every build — visible in CI and code review.
                            Publishers can also raise it to an error at pack time.
                        </p>
                        break;
                }
                break;

            case OutputStep:
                <ConsumerOutputPanel Model="model" />
                break;
        }
    </div>

    <div class="wizard-nav">
        <span>
            @if (step > 0)
            {
                <button type="button" @onclick="Back">Back</button>
            }
        </span>
        <span>
            @if (step < OutputStep)
            {
                <button type="button" class="primary" disabled="@(!CanAdvance)" @onclick="Next">
                    @(step == OutputStep - 1 ? "Generate" : "Next")
                </button>
            }
            else
            {
                <button type="button" @onclick="Restart">Start over</button>
            }
        </span>
    </div>
</div>
