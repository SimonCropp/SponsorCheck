=== Snippet (Global properties (owner mode)) ===
<PropertyGroup>
  <acme_GitHubSponsorAccount>octocat</acme_GitHubSponsorAccount>
  <acme_SponsorshipPrivateUntil>2027-05</acme_SponsorshipPrivateUntil>
</PropertyGroup>

=== File to edit ===
Directory.Build.props (covers every project under it) — or any one consuming project's .csproj.

=== Instruction ===
Add the properties below to a <PropertyGroup>. One declaration covers every package from owner 'acme'; the property names are exact (the owner prefix is baked into the package).

=== Build outcome ===
Private (GitHub Sponsors) and incognito (Open Collective) sponsorships are deliberately excluded from the bundled hash list, so there is nothing for the verifier to match — the declaration is trusted instead. The build passes through the end of 2027-05 (UTC) and logs a high-priority SC059 audit message naming the account and the end month. After that it fails with SC058 until the claim is renewed, which is the point: a private sponsorship that quietly lapsed should stop working rather than ride along forever. The publisher caps the claim (the default is 12 months, and this package could not be inspected to confirm), so a month further out fails with SC055. A value that isn't 'yyyy-MM' fails with SC052.

=== Notes ===
- The bundled list is frozen per package version: versions that pass keep passing even if sponsorship later lapses; a lapse surfaces when upgrading to a version packed after it.
- The properties cover every package from owner 'acme' — one declaration for the whole family. The exact property names are also printed in the SC021 error message.

=== Markdown ===
# Configure SponsorCheck licensing for ThePackage

Generated by the [SponsorCheck setup wizard](https://simoncropp.github.io/SponsorCheck/consumer).

Context: the build references `ThePackage` 1.2.3, which bundles the [SponsorCheck](https://github.com/SimonCropp/SponsorCheck) build-time sponsorship verifier, and requires a license-mode declaration on every build.

- Chosen mode: sponsor account match
- Placement: owner mode — global MSBuild properties prefixed 'acme_'

## Change to make

File to edit: Directory.Build.props (covers every project under it) — or any one consuming project's .csproj.

Add the properties below to a <PropertyGroup>. One declaration covers every package from owner 'acme'; the property names are exact (the owner prefix is baked into the package).

```xml
<PropertyGroup>
  <acme_GitHubSponsorAccount>octocat</acme_GitHubSponsorAccount>
  <acme_SponsorshipPrivateUntil>2027-05</acme_SponsorshipPrivateUntil>
</PropertyGroup>
```

## Expected build outcome

Private (GitHub Sponsors) and incognito (Open Collective) sponsorships are deliberately excluded from the bundled hash list, so there is nothing for the verifier to match — the declaration is trusted instead. The build passes through the end of 2027-05 (UTC) and logs a high-priority SC059 audit message naming the account and the end month. After that it fails with SC058 until the claim is renewed, which is the point: a private sponsorship that quietly lapsed should stop working rather than ride along forever. The publisher caps the claim (the default is 12 months, and this package could not be inspected to confirm), so a month further out fails with SC055. A value that isn't 'yyyy-MM' fails with SC052.

Notes:

- The bundled list is frozen per package version: versions that pass keep passing even if sponsorship later lapses; a lapse surfaces when upgrading to a version packed after it.
- The properties cover every package from owner 'acme' — one declaration for the whole family. The exact property names are also printed in the SC021 error message.

## Reference

- Consumer guide: https://github.com/SimonCropp/SponsorCheck/blob/main/docs/ConsumerUsage.md
- Diagnostic codes: https://github.com/SimonCropp/SponsorCheck/blob/main/docs/VerifierDiagnosticCodes.md
