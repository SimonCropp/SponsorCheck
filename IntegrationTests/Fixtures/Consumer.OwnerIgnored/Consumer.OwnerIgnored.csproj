<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <!-- Opt out via the global property: owner mode allows the build but emits the SC023 warning. -->
    <SponsorshipLicenseIgnored>true</SponsorshipLicenseIgnored>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="ThePackageOwnerMode" Version="1.0.0" />
  </ItemGroup>
</Project>
