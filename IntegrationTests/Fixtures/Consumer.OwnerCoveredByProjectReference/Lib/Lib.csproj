<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <!-- Owner-mode sponsorship set in the project that DIRECTLY references the package. The
         parent project (no direct ref, no property) relies on this Lib's verifier covering it. -->
    <acme_GitHubSponsorAccount>alice</acme_GitHubSponsorAccount>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="ThePackageOwnerModeTransitive" Version="1.0.0" />
  </ItemGroup>
</Project>
