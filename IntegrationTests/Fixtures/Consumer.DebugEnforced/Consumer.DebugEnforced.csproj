<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>
  <ItemGroup>
    <!-- No license metadata; verifier should fail with SC001 in Debug, same as Release. -->
    <PackageReference Include="ThePackage" Version="1.0.0" />
  </ItemGroup>
</Project>
