<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>
  <ItemGroup>
    <!-- No license metadata; would fail in Release but should be no-op in Debug. -->
    <PackageReference Include="ThePackage" Version="1.0.0" />
  </ItemGroup>
</Project>
